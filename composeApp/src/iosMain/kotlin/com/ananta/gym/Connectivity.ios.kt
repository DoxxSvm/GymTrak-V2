package `in`.gym.trak.studio.network

actual fun isNetworkAvailable(): Boolean {
    // Basic synchronous check for iOS (using reachability is standard but requires more code)
    // For now, return true as a placeholder or implement a simple check if possible.
    return true 
}
